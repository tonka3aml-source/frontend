/* import React from "react";

const FeaturedImg = ({ page, size="full", fallback }) => {

  const selectedImg =
    page.embedded["wp:featuremedia"][0].media_details.sizes.full.source_url || fallback - ako nema slike prikazat će se fallback

  return (
    <>
      <img src={selectedImg } className?="hero_img" />
    </>
  );
};

export default FeaturedImg;
 */

//ovo je način umetanja glavne slike ispred svega na stranici
